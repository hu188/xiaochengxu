<view class="i-class i-badge">
    <slot></slot>
    <view class="i-badge-dot" a:if="{{ dot }}"></view>
    <view class="i-badge-count i-class-alone" a:elif="{{ count*1 !== 0 }}">{{ count }}</view>
</view>
